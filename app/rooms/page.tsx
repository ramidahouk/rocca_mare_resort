export default function RoomsPage() {
  return (
    <main>
      <section style={{ minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center" }}>
        <p className="type-label">Rooms — coming soon</p>
      </section>
    </main>
  );
}
